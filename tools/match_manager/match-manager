#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

if [ ! -f dist/cli.js ] || [ src/cli.tsx -nt dist/cli.js ] || [ src/ui.tsx -nt dist/cli.js ] || [ src/core.ts -nt dist/cli.js ]; then
  npm run build >/dev/null
  chmod +x dist/cli.js
fi

exec node dist/cli.js "$@"
